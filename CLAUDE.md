# Claude Instructions

## Git Workflow
- Always merge changes into `main` when a task is complete — do not wait to be asked.
- If working in a worktree, commit the changes there first, then merge into `main` before finishing.
